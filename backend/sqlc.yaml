version: "2"
sql:
  - engine: postgresql
    schema: "sql/migrations/"
    queries: "sql/queries/"
    gen:
      go:
        package: "db"
        out: "internal/repository/db"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
